<% if true %>
  <div class="mb-6 space-y-3">
    <%= render "layouts/shared/notice", message: "We initialized a resume example for the onboarding." %>
    <%= render "layouts/shared/notice", message: "Click 'Back to resume'" %>
  </div>
<% end %>
<div class="flex flex-col md:flex-row justify-between items-center mb-6">
  <h1 class="font-bold text-4xl">Editing resume</h1>
  <%= link_to "Back to resumes", resumes_replicas_path, class: "rounded py-2 px-4 bg-gray-100 inline-block font-medium border-2 border-black mt-3 md:mt-0" %>
</div>
<hr>
<%= render "form", resume: @resume %>
